apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: dev-

patches:
  - target:
      kind: ConfigMap
      name: mining-management-web-config
    patch: |
      - op: replace
        path: /data/config.json
        value: |
          {
            "apiUrl": "https://dev-api.mining.example.com",
            "production": false,
            "appTitle": "Mining Management System (Dev)"
          }

  - target:
      kind: Deployment
      name: mining-management-web
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
